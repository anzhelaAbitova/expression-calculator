function eval() {
    // Do not use eval!!!
    return;
}

function expressionCalculator(str) {
    let expr = String(str);
    let result = 0;
    return result;
}

module.exports = {
    expressionCalculator
}